# API Flask dependencies
flask>=2.3.0
flask-cors>=4.0.0

# Core dependencies
pydantic>=2.0.0
typing-extensions>=4.0.0

# Machine Learning and AI
weaviate-client>=4.0.0
gradio-client>=0.8.0
instructor>=1.0.0
groq>=0.4.0
numpy>=1.24.0

# Database
supabase>=2.0.0

# Text processing
unidecode>=1.3.0
PyYAML>=6.0

# HTTP requests
requests>=2.31.0

# Logging and utilities
python-dotenv>=1.0.0

# WSGI server for production (Render recommends gunicorn)
gunicorn>=20.1.0

# Fix Protobuf version conflicts
protobuf>=4.21.0,<5.0.0
grpcio>=1.50.0,<2.0.0

# Standard library (included in Python)
# json, re, unicodedata, sys, os, warnings, datetime, argparse, enum
